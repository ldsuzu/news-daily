# 每日领域新闻推送本地软件 · 技术框架 v0.4（定稿）

> **v0.1 → v0.4 决策链**
> - 交付形态：**一个本地软件**（自带后台抓取 + 阅读界面），不做 IM/邮件推送。
> - 网络前提：本地能上普通互联网，上不了被墙站点。
> - 内容风格：中文摘要为主 + 保留英文原标题。
> - **条数不设硬上限**：改为「精选 / 全部」两层，精选每领域默认 10 条、可调（见 §4.1）。
> - **历史永久可访问**：不做滚动过期，全部可检索可回溯（见 §4.2）。
> - 海外采集分身：**GitHub Actions 定时任务（0 元）**。
> - 软件形态：**阶段 1 本地服务 + 浏览器界面**，阶段 2 再决定要不要加桌面外壳。
> - 界面原型：`prototype/today.html`（3 个版式变体，见 §5.1）。
> - **界面版式已选定：变体 A（报纸版式）** —— 报头 + 头条 + 三栏次条 + 多栏列表。
> - 实现进度：**M0 已完成**（代码见 `src/newspipe/`，`README.md` 有上手命令）。

---

## 1. 核心矛盾与解法（一句话版）

**采集时刻 ≠ 阅读时刻。** 软件跑在你的机器上，出口就是你家的网络，翻不过去。所以：

- **被墙源** → 由墙外的 GitHub Actions 抓，产出 JSON；
- **国内可达源** → 本地直连抓；
- **两边在本地合并、去重、加工、展示**；
- 原文正文在抓取那一刻就落进本地库 —— **这是"开 VPN 之前就能读"的物理基础**。

对你来说仍然"就是一个软件"：GitHub Actions 只是它一个不需要你操心的抓取分身。

---

## 2. 架构总览

```
┌──────────── 本地软件（单进程，127.0.0.1:8787）────────────────────┐
│  阅读界面   今日日报 / 时间线 / 源筛选 / 搜索 / 收藏 / 已读          │
│  后台服务   调度器 → 采集器 → 处理管线 → SQLite → 生成日报           │
│  设置界面   源开关与权重 / 每领域上限 / 抓取时间 / 代理 / 节点地址     │
└──────┬──────────────────────────────┬───────────────────────────┘
       │ ① 直连抓取                    │ ③ 定时拉取 bundle JSON
       ▼                              ▼
 机核 / 游研社 / B站游戏区        [GitHub Actions 采集分身]
 量子位 / 机器之心 …              每天 06:30(北京) 跑一次
                                  IGN / PC Gamer / Gematsu /
       │                          arXiv / OpenAI / HF / HN …
       └──────────► ② 合并入库 ◄──────────┘
                        │
                        ▼
     ④ 规范化 → ⑤ 去重聚类 → ⑥ 打分分类 → ⑦ 中文摘要 → ⑧ 生成日报
```

**接口即边界**：海外分身只负责"把原文抓回来"，所有加工都在本地。换节点、换源、改算法都不用碰另一侧，而且断网也能重跑整条管线。

---

## 3. GitHub Actions 采集分身（详细设计）

### 3.1 分工规则

每个源在 `sources.yaml` 里有一个标记：

```yaml
- id: ign
  name: IGN
  domain: game
  type: rss
  url: https://feeds.feedburner.com/ign/all
  needs_proxy: true      # ← 墙外源，交给 GH Actions
  weight: 1.0
  enabled: true

- id: gcores
  name: 机核
  domain: game
  type: rss
  url: https://www.gcores.com/rss
  needs_proxy: false     # ← 国内可达，本地直连
  weight: 1.2
  enabled: true
```

`collector` 模式只抓 `needs_proxy: true` 的源（减少 Actions 运行时间与流量），`standalone` 模式只抓 `needs_proxy: false` 的源。**两边零重叠，本地合并时天然不冲突。**

### 3.2 Workflow 设计

```yaml
# .github/workflows/collect.yml
name: collect
on:
  schedule:
    - cron: '30 22 * * *'   # UTC 22:30 = 北京 06:30，给本地 07:00 留 30min 缓冲
  workflow_dispatch:         # 支持手动补跑
jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: pip install -e .
      - run: newspipe collect --mode collector --out bundles/
      - run: |
          git config user.name  "newspipe-bot"
          git config user.email "bot@users.noreply.github.com"
          git add bundles/
          git commit -m "bundle $(date -u +%F)" || echo "no changes"
          git push
```

要点：

- **cron 用 UTC**。GH Actions 的 schedule 有几分钟到几十分钟的调度延迟（高峰期更明显），所以提前到北京 06:30 跑，本地 07:00 拉取时基本一定已就绪。
- **免费额度**：公开仓库无限；私有仓库 2000 分钟/月，每天跑 2 分钟完全够。
- **失败不影响你**：本地拉不到新 bundle 时走降级逻辑（见 §6）。

### 3.3 数据契约（bundle JSON）

这是本地与海外之间**唯一的接口**，必须先钉死：

```json
{
  "version": 1,
  "date": "2025-06-01",
  "generated_at": "2025-06-01T22:35:12Z",
  "mode": "collector",
  "sources": [
    { "id": "ign", "name": "IGN", "domain": "game", "ok": true, "count": 42, "error": null }
  ],
  "items": [
    {
      "source_id": "ign",
      "domain": "game",
      "url": "https://www.ign.com/articles/...",
      "url_hash": "sha256:9f2c...",
      "title": "Original English title here",
      "title_en": "Original English title here",
      "published_at": "2025-06-01T18:00:00Z",
      "fetched_at": "2025-06-01T22:35:12Z",
      "lang": "en",
      "excerpt": "RSS 里带的短摘要",
      "content_text": "已抽取并截断（≤20k 字符）的正文..."
    }
  ]
}
```

- `url_hash` 由本地和海外用**同一段代码**计算 → 合并去重零歧义，重复拉取也幂等。
- 文件按 `bundles/YYYY-MM-DD.json` 命名，历史 bundle 留在仓库里，等于自带一份异地备份。

### 3.4 本地怎么把 bundle 拿回来（关键：GitHub 在国内不稳）

`raw.githubusercontent.com` 、GitHub Pages 在国内时通时不通。所以本地同步走**多源回退**：

```
① jsDelivr CDN     https://cdn.jsdelivr.net/gh/<user>/<repo>@main/bundles/<date>.json
② GitHub API       https://api.github.com/repos/<user>/<repo>/contents/...  (带 token)
③ 直连 raw         https://raw.githubusercontent.com/...
④ 本地已有 bundle   data/bundles/ 里上次成功拉到的
```

按顺序尝试，第一个成功即写入 `data/bundles/` 并入库。**全部失败时用最近一次成功的 bundle**，界面上标"⚠️ 海外节点数据为 X 小时前"，绝不让你打开软件看到空白。

### 3.5 隐私

bundle 里只有公开新闻的标题、正文、链接 —— 本身不含隐私。若仍不想公开，用**私有仓库 + fine-grained PAT（只读 contents 权限）**，token 存在本地设置的 `settings.yaml` 里，不进 git。

---

## 4. 处理管线

| 步骤 | 做法 | 要点 |
|---|---|---|
| ① 规范化 | URL 去 `utm_*`/`ref`/`fbclid`；标题全角半角统一、去空白 | 保证同一文章不同来源能对上 |
| ② 正文抓取 | `trafilatura` 抽正文存本地（海外侧抓，本地侧也抓） | **不可跳过**：这是"没网也能读"的物理基础 |
| ③ 去重 | URL 指纹 → 标题 SimHash → 跨源同事件聚类（embedding 余弦 >0.85） | 簇内取源权重最高者为代表，其余折叠成"另有 N 家报道" |
| ④ 打分分类 | 规则优先（源权重、关键词、时效衰减），LLM 兜底 `{领域, 重要度0-10, 一句话中文摘要}` | 结构化 JSON 返回，失败可降级为纯规则 |
| ⑤ 摘要 | 外网条目：**中文一句话摘要 + 3 条要点**，**英文原标题保留**；中文源直接用原文摘要 | 满足"不开 VPN 也读得懂 + 可检索" |
| ⑥ 日报 | 生成 `archive/YYYY-MM-DD-{game,ai}.md`，并算出当天的「精选」集合 | 文件本身就是可备份、可搜索的归档 |

**③ 是「精选」能成立的前提**：同一件事被 10 家报道只占 1 条名额，精选才装得下一天的真正信息量。

**成本控制**：只对新增条目调 LLM；正文截断 3k token；相同内容摘要缓存；每日预算硬上限。

### 4.1 条数：精选 / 全部 两层，而不是硬上限

「每领域 10 条」当上限用会把信息拦掉，当精选用才合理。所以拆成两层：

| 层 | 内容 | 数量 | 用途 |
|---|---|---|---|
| **精选** | 重要度 ≥ 阈值（默认 8 分） | 每领域默认 10 条，**可调、可关** | 每天只扫一眼时的默认视图 |
| **全部** | 当天入库的所有条目，按时间倒序 | **不设上限** | 想深挖时切过去，永远不丢东西 |

- 阈值和条数都在设置页可调（「精选每领域条数」「精选最低分」两个旋钮）。
- 界面每屏都提供 `精选 / 全部` 切换；默认落在精选，但只有一个点击的距离。
- **未进精选 ≠ 被丢弃**：它照常入库、照常可搜、照常出现在「全部」与「历史」里。精选只是排序视图，不是过滤器。
- 日报 Markdown 归档里同时写精选区和完整清单，避免归档也被截断。

### 4.2 历史：永久保留 + 全文检索

历史不是"翻页"，是一等公民：

- **不做滚动过期**。条目、正文、摘要全部留在 SQLite 里（每天的正文缓存约几 MB，一年量级完全可接受）。
- **SQLite FTS5 全文检索**：建 `items_fts` 虚拟表（标题中文/英文 + 摘要 + 正文），支持按关键词搜全部历史，中文用 `trigram` 分词器。
- **多维回溯入口**：按日期（日历/前后一天）、按领域、按来源、按分值区间、按关键词、按收藏、按已读状态。
- **归档文件同步**：`archive/*.md` 与库内容一致，等于一份纯文本备份，随时可 grep、可丢进 git。
- 界面上的历史入口固定在主屏（日期导航 + 侧栏日期列表 + 搜索框），不需要先进某个"历史页"才能找东西。

---

## 5. 存储与界面

### SQLite 单文件（零运维，全在你机器上）

| 表 | 关键字段 |
|---|---|
| `sources` | id, name, domain, type, url, weight, enabled, needs_proxy, last_ok_at, fail_count |
| `items` | id, source_id, domain, url, url_hash, title, title_en, title_simhash, published_at, fetched_at, lang, excerpt, content_text, summary_cn, score, origin(local/remote) |
| `clusters` | id, cluster_key, item_ids(json), representative_item_id, size |
| `digests` | id, date, domain, item_ids(json), markdown |
| `read_state` | item_id, read_at, starred |
| `fetch_log` | id, source_id, started_at, ok, count, error |
| `items_fts` | FTS5 虚拟表（title, title_en, summary_cn, content_text），trigram 分词 —— 支撑全历史搜索 |

### 界面四屏

1. **今日**（主屏）：领域 Tab（游戏 / AI / 全部）+ `精选 / 全部` 切换；每条 —— 来源徽标 · 发布时间 · 中文摘要 · 重要度 · **英文原标题** · 原文链接 · "本地正文"展开；顶部显示今日条数 / 精选条数 / 抓取状态 / 海外数据新鲜度 / 日期导航（← 昨天 · 今天 · 明天 →）/ 手动刷新。**日期导航就长在首屏上**，往前往后点一下就是历史。
2. **历史**：日历或日期列表 + 搜索框（FTS5 全历史）+ 筛选（领域 / 来源 / 分值 / 收藏 / 已读）；结果列表可排序，点开与今日同一套阅读面板。
3. **信息源**：表格 —— 名称、领域、类型、来源归属（本地/海外）、上次成功、连续失败、权重、启用开关、"立即抓取"。**这就是需求 1「包含信息源」的落地面**。
4. **设置**：抓取时间表、**精选每领域条数（默认 10）与最低分阈值（默认 8，均可调可关）**、代理地址（可选）、海外节点地址与 token、LLM 开关与每日预算、数据目录、导出 / 备份归档。

> 条数旋钮放在设置里而不是写死在代码里：今天想看 5 条，明天想看 30 条，都应该是改一个数字的事。

### 5.1 界面原型（先看再写）

`prototype/today.html` —— 双击即可打开的单文件原型，把「今日」屏做了 **3 个结构完全不同的版式变体**，与「精选/全部」切换、历史入口、来源徽标、英文原标题保留等设计一起摆出来：

| 变体 | 版式 | 特点 |
|---|---|---|
| **A — 报纸版式** | 报头 + 头条 + 三栏次条 + 多栏列表 | 版式驱动信息层级，一眼看出"今天什么最重要"，密度高、有读报感 |
| **B — 三栏工作台** | 左：领域与历史日期 / 中：条目列表 / 右：阅读面板 | RSS 阅读器范式，左栏直接展示历史回溯与来源筛选，中栏列表即"全部" |
| **C — 时间流** | 竖向时间轴，按时刻推进 | 强调"今天发生了什么"的节奏；顶部统计与 chips 直接承载条数切换 |

切换方式：底部浮动栏 ← → 或键盘左右键，URL 参数 `?variant=A|B|C` 可分享。**数据全是写死的示例数据**，不接后端、不落任何持久化 —— 它的唯一目的是让你在写代码前先挑版式。

**结论：采用变体 A（报纸版式）。** A 的信息层级最直接 —— 一眼看出今天什么最重要，且不需要额外交互就能扫完全部；B 的三栏在窗口变窄时会被挤扁，C 的时间轴好看但把"重要性"摊平了。A 里要保留另外两个变体值得留的两样东西：**报头的日期导航**（历史入口）与**页脚的「查看全部」**（条数切换）。

---

## 6. 调度与容错

- **节奏**：北京 07:00 拉 bundle + 抓国内源 + 生成日报（时间可配）；白天每 2 小时增量抓热榜类源；软件启动时若当天日报缺失立即补一次。海外侧北京 06:30 跑。
- **断网降级**：抓取失败**不生成空日报**，界面顶部提示"⚠️ 3 个源抓取失败"，昨日日报照常可读。
- **海外节点失联**：四层回退（§3.4）；仍失败则沿用最近 bundle 并显式标注数据时间。
- **单源隔离**：单源失败不影响其它源；重试 3 次指数退避；连续失败 ≥3 天在信息源页标红。
- **幂等**：`url_hash` 唯一约束 —— 重复运行、重复拉取都不产生重复条目。
- **离线优先**：去重、打分、摘要、生成日报全部不依赖网络访问原文。

---

## 7. 技术栈（定稿）

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.12 | 爬虫/抽取/LLM 生态无可替代 |
| 拉取 | `httpx` + `feedparser` | 异步、连接池、超时重试成熟 |
| 正文抽取 | `trafilatura` | 通用正文抽取效果最好 |
| 调度 | `APScheduler` | 进程内即可，不引入 Airflow |
| 存储 | SQLite（标准库） | 单文件、可备份、可查询 |
| Web 后端 | FastAPI + Uvicorn | 轻、异步、自带文档 |
| 前端 | 原生 HTML/CSS/JS（无构建步骤） | 三屏界面不需要框架，省掉整条前端工具链 |
| 摘要 | DeepSeek API | 便宜、中文好，200 条/天约几分钱 |
| 海外采集 | GitHub Actions | 0 元、零运维 |
| 兜底抓取 | 自建 RSSHub（可选，Docker） | 无 RSS 的源统一成 RSS，采集器只写一种适配器 |
| 打包 | PyInstaller（阶段 2） | 单 exe |

---

## 8. 目录结构

```
news-daily/
├── .github/workflows/collect.yml   # 海外采集分身
├── prototype/today.html     # 界面原型（3 个版式变体，选完即弃）
├── config/
│   ├── settings.yaml        # 抓取时间/上限/代理/节点token/LLM预算
│   ├── domains.yaml         # 领域定义：game / ai
│   └── sources.yaml         # 信息源清单（weight / enabled / needs_proxy）
├── src/newspipe/
│   ├── collectors/          # rss.py api.py html.py rsshub.py bundle.py
│   ├── pipeline/            # normalize.py extract.py dedupe.py score.py digest.py
│   ├── llm/                 # prompt + 结构化输出 + 成本统计
│   ├── storage/             # schema.sql + repo.py
│   ├── web/                 # routes.py + templates/ + static/
│   ├── modes.py             # standalone / collector
│   └── cli.py               # serve fetch collect sync process digest doctor
├── data/
│   ├── news.db
│   ├── contents/            # 正文缓存，按日期分目录
│   └── bundles/             # 本地留存的 bundle 副本（回退用）
├── archive/                 # 每日 Markdown 归档
└── tests/
```

CLI：`fetch`（抓本地源）· `collect`（海外模式，只抓 needs_proxy 源并导出）· `sync`（拉 bundle）· `process`（规范化+去重+打分+摘要）· `digest`（生成日报）· `serve`（开界面）· `doctor`（源健康检查）。

---

## 9. 信息源清单（起步 20 个）

### 游戏（10）

| 源 | 类型 | 归属 | 直连 |
|---|---|---|---|
| IGN | 综合 | 海外 | ✗ |
| GameSpot | 综合 | 海外 | ✗ |
| Eurogamer | 综合 | 海外 | ✗ |
| PC Gamer | PC | 海外 | ✗ |
| Rock Paper Shotgun | PC/独立 | 海外 | ✗ |
| Gematsu | 日式/亚洲 | 海外 | ✗ |
| 4Gamer / Famitsu | 日本 | 海外 | ✗ |
| VGC | 综合 | 海外 | ✗ |
| Nintendo Life | 平台向 | 海外 | ✗ |
| Steam 新闻（Web API） | 官方公告 | 海外 | 部分 |
| 机核 GCORES | 中文 | 本地 | ✓ |
| 游研社 | 中文 | 本地 | ✓ |
| B站 游戏区热门 | 视频/热度 | 本地 | ✓ |

### AI（10）

| 源 | 类型 | 归属 | 直连 |
|---|---|---|---|
| arXiv（cs.AI / cs.CL / cs.LG） | 论文 | 海外 | ✗ |
| Hacker News（AI 关键词过滤） | 讨论 | 海外 | ✗ |
| OpenAI / Anthropic / DeepMind 博客 | 一手 | 海外 | ✗ |
| Hugging Face Blog / Daily Papers | 一手 | 海外 | ✗ |
| Google Research / MSR Blog | 一手 | 海外 | ✗ |
| The Verge AI / TechCrunch AI | 媒体 | 海外 | ✗ |
| Import AI / The Batch | Newsletter | 海外 | ✗ |
| 量子位 | 中文 | 本地 | ✓ |
| 雷峰网 | 中文 AI | 本地 | ✓ |
| InfoQ 中文 | 中文 | 本地 | ✓ |
| Solidot | 科技 | 本地 | ✓ |
| 开源中国 | 开源 | 本地 | ✓ |
| 少数派 | 科技 | 本地 | ✓ |
| 机器之心 | 中文 | 本地（需自建 RSSHub） | ✓ |

> **实测修正（2026-09-22）**：国内游戏媒体**基本不提供 RSS** —— 游民星空 403、3DM 403、
> indienova / VGtime / 电玩巴士 / 篝火营地全部 404、A9VG 超时。所以游戏侧的本地直连源
> 实际只有 机核、游研社、Steam 公告，外加一个 B站游戏区排行榜（走官方 API）。
> AI 侧则宽裕得多，上面 5 个国内源实测全部有效（InfoQ 20 条、雷峰网 20 条、开源中国 50 条、
> Solidot 20 条、少数派 10 条）。
> **换言之：游戏领域对外网源的依赖比 AI 更重** —— 这也是海外分身不可省的原因。

> 起步建议先各挑 4~5 个高信噪比源跑通，再逐步加。源越多，聚类去重的价值越明显。

---

## 10. 里程碑

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **M0** 骨架 | **先看原型选版式** + 项目结构 + SQLite schema（含 FTS5）+ 3 个源（本地/海外各 1 + 1 个通用）+ `fetch` 命令 | 版式定了；一条命令抓到 3 个源条目并入库 |
| **M1** 可用 | 20 个源 + 正文抓取 + 去重 + 今日屏（含精选/全部切换与日期导航）+ 历史屏（FTS 搜索）+ 信息源屏 + GH Actions 接入 | **不开 VPN 打开软件就能读完当天外网新闻，并能一路搜回历史** ← 核心需求达成 |
| **M2** 好读 | LLM 中文摘要/打分 + 精选阈值与条数旋钮 + Markdown 归档 + 定时自动化 | 每天 07:00 自动备好一份精选日报，且完整清单一条不丢 |
| **M3** 打磨 | 聚类展示、收藏、关键词关注/屏蔽、源权重自学习、可选桌面外壳 | 从"能看"到"每天想打开" |

---

## 11. 风险与对策

| 风险 | 对策 |
|---|---|
| 被反爬 / 被封 | 标准 UA、单域限速 ≥1 req/s、失败退避；优先官方 RSS/API；GH runner 出口是云 IP，遇 Cloudflare 拦截源改用 RSSHub 兜底 |
| 版权 | 默认只存**中文摘要 + 短引用 + 链接**；全文仅在本地作私人阅读缓存，archive 不对外分发 |
| LLM 成本 | 只处理新增条目 + 截断 + 缓存 + 每日预算硬上限 |
| 源失效/改版 | `doctor` 健康检查 + 连续失败标红；RSSHub 路由失效时降级 HTML 解析 |
| 信息过载 | 聚类去重 + 精选/全部两层（低分不占精选名额，但绝不被丢弃）+ 精选条数与阈值旋钮 |
| GH Actions 定时不准/失败 | 提前 30min 跑 + 多层镜像回退 + 沿用最近 bundle 并显式标注新鲜度；保留 VPS 升级路径（同一套代码换个地方跑） |

---

## 12. 进度与下一步

**M0 / M1 已完成**（版式选定 A）。

| 已交付 | 位置 | 验证情况 |
|---|---|---|
| SQLite schema（FTS5 trigram 索引 + 触发器 + 存量回填） | `src/newspipe/storage/schema.sql` | 中文全文检索命中正常 |
| 采集器：RSS / arXiv / Hacker News / Steam，HTTP 层统一 UA·限速·代理 | `src/newspipe/collectors/` | 122 条入库，重复抓取新增 0 条 |
| 规范化与 `url_hash`（本地与海外共用，跨机器一致） | `pipeline/normalize.py` | 契约测试覆盖 |
| 正文抽取（trafilatura） | `pipeline/extract.py` | 58/122 条够离线精读 |
| 去重三层漏斗（URL 指纹 → 标题 SimHash → 跨源聚类） | `pipeline/dedupe.py` | 6 项测试覆盖 |
| 规则打分（源权重 + 时效 + 关键词） | `pipeline/score.py` | 契约测试覆盖 |
| bundle 契约 + 四层镜像回退 | `pipeline/bundle.py` · `sync.py` | 端到端 sync 已验证 |
| CLI：`fetch` `collect` `sync` `process` `serve` `doctor` `stats` `search` `dates` | `cli.py` | — |
| 四屏界面（版式 A：头条 + 三栏次条 + 多栏列表） | `src/newspipe/web/` | 8 项页面检查通过 |
| GitHub Actions 采集分身 | `.github/workflows/collect.yml` | **待你的仓库** |
| 测试 **23 项全绿** | `tests/` | `pytest -q` |

**实测结果**：

- 本地可达源扩到 **10 个**，共 **292 条**入库（今日 91 条）；重复抓取新增 0 条（幂等成立）；
- 中文全文检索命中正常，历史日期自然积累；
- 正文抽取实测补到 30 条（平均 689 字），够离线精读；
- **聚类在真实数据上生效**：扫描 169 条 → 2 个簇、4 条重复报道被折叠；
- `sync` 的合并链路端到端验证过：bundle 回退可走通，合并后来源可区分（`local` / `remote`）；
- 墙外源在本地直连**全部超时卡死** —— 这实证了海外分身的必要性。

**本轮踩到并修掉的两个真问题**：

1. **精选的语义错了**。一开始把精选写成"重要度 ≥ 8 分才进来"，而规则打分的实际分布是
   6~7 分，于是默认视图几乎是空的（5.7KB vs 全部视图的 22KB）。改成 **Top N**：
   精选 = 按重要度取每领域前 N 条，阈值降级为可选下界（默认 0）。
2. **重复条目白占精选名额**。雷峰网给同一篇文章发了两个**完全不同的 slug**
   （`.../4V3AYUarwL4RCE5O.html` 和 `.../l4HSONbqE9vSaA4z.html`），URL 规范化救不了 ——
   这恰好证明标题 SimHash 那一层是必需的。但聚类之后还得把非代表条目从视图里挡掉，
   于是加了 `representatives_only`：重复条目照常留在库里可搜，只是不占精选名额。

**唯一还没打通的一环**：海外分身的真实运行需要你自己的 GitHub 仓库。代码、workflow、
回退链路都已就位，但这一步只有你能做：

1. 建一个仓库（私有也行），把本项目推上去；
2. 在 `config/settings.yaml` 里填 `bundle.repo`（形如 `yourname/news-bundles`）；
3. 手动触发一次 workflow，然后本地跑 `newspipe sync`。

跑通这三步，M1 的核心验收（**不开 VPN 打开软件就能读完当天外网新闻，并能一路搜回历史**）
才算真正达成 —— 在此之前，界面里能读到的还只有国内可达源的内容。

**M2 进展**：每日 Markdown 日报归档 ✅、定时自动化 ✅（`newspipe run` 一条命令跑完
sync → fetch → process → digest，`newspipe schedule install` 注册成每日任务）。
仍待做：LLM 中文摘要与打分 —— 那时 `picks_min_score` 才会重新变成一个有用的阈值。

**本轮（round 3）修掉的问题**：

1. **去重 URL 和展示 URL 是两件事，我一开始混用了**。`canonical_url` 会去掉 `www.`，
   用来算指纹没问题，但把它当链接存进库就有风险 —— 游研社的证书**只对 `www.yystv.cn` 有效**，
   去掉 www 后链接直接证书错误。现在拆成 `canonical_url`（只给去重）与 `strip_tracking`
   （保留 host，给人点），并有测试钉住这个区别。
2. **有些源根本抽不出正文**。机核、开源中国的文章页是 SPA / 空壳 —— 实测机核返回 151KB HTML、
   trafilatura 抽出 **0 字**，带不带 www 都一样。给 `sources.yaml` 加 `extract: false`
   标记后，`process` 不再对它们徒劳发请求，补正文从每轮 2~3 条变成 **24 条**。
   这也说明：**"离线可读全文"对 SPA 站点做不到**，那边只能靠 RSS 摘要 + 原文链接。
3. **B站 API 的风控很反直觉**：带 `Referer` 反而返回 `-352`，不带任何自定义头却稳定 `code=0`。
   已按实测调整。
4. **采集缺重试**。限速、风控、偶发 5xx 都是间歇性的，一次失败就记黑账太苛刻 —— 加了退避重试。
